# ForgeLM

Train, fine-tune & serve a small edge/on-device language model from scratch.

- Docs: `docs/architecture.md`, `docs/modules.md`, `docs/changelog.md`
- Install: `uv sync --group dev` | Test: `uv run pytest` | Lint: `uv run ruff check .`